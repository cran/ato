% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zenodo.R
\name{ato_deposit_zenodo}
\alias{ato_deposit_zenodo}
\title{Prepare a Zenodo deposit payload for the session manifest}
\usage{
ato_deposit_zenodo(
  title = NULL,
  description = NULL,
  creators = list(list(name = "Anonymous")),
  keywords = c("ATO", "taxation", "Australia", "reproducibility"),
  upload = FALSE,
  sandbox = FALSE,
  token = Sys.getenv("ZENODO_TOKEN")
)
}
\arguments{
\item{title}{Deposit title. Defaults to "ATO data snapshot
YYYY-MM-DD" using the current snapshot pin.}

\item{description}{Free-text description. Defaults to a short
auto-generated note listing the datasets fetched.}

\item{creators}{List of creator records. Each should be a list
with \code{name}, optional \code{affiliation}, \code{orcid}. Defaults to a
single anonymous entry; override for published work.}

\item{keywords}{Character vector of keywords. Defaults to
\code{c("ATO", "taxation", "Australia", "reproducibility")}.}

\item{upload}{Logical; if \code{TRUE}, POSTs the deposit to Zenodo
and uploads the manifest CSV. Default \code{FALSE} (dry run).}

\item{sandbox}{Logical; if \code{TRUE}, uses Zenodo Sandbox
(sandbox.zenodo.org) for testing. Default \code{FALSE}.}

\item{token}{Zenodo personal access token. Defaults to
\code{Sys.getenv("ZENODO_TOKEN")}.}
}
\value{
A list with \code{payload} (the JSON metadata), \code{manifest_path}
(where the CSV manifest was staged), and if \code{upload = TRUE},
\code{deposit_id}, \code{doi_prereserve}, and \code{url}.
}
\description{
Builds the JSON metadata payload Zenodo expects for a data
deposit, using the current \code{ato_manifest()} and the snapshot
pin set via \code{\link[=ato_snapshot]{ato_snapshot()}}. The function does NOT upload by
default; it returns the payload and saved manifest path so
you can inspect before calling with \code{upload = TRUE}.
}
\details{
To upload, supply a Zenodo personal access token via the
\code{ZENODO_TOKEN} environment variable (or the \code{token} argument).
Tokens can be generated at
\url{https://zenodo.org/account/settings/applications/}.
}
\examples{
\donttest{
ato_snapshot("2026-04-24")
ato_deposit_zenodo(
  title = "ATO data snapshot for working paper v1",
  creators = list(list(name = "Coverdale, Charles")),
  upload = FALSE
)
}
}
\seealso{
Other reproducibility: 
\code{\link{ato_manifest}()},
\code{\link{ato_manifest_clear}()},
\code{\link{ato_manifest_write}()},
\code{\link{ato_sha256}()},
\code{\link{ato_snapshot}()}
}
\concept{reproducibility}
