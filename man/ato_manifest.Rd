% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{ato_manifest}
\alias{ato_manifest}
\title{Return the session manifest of fetched ATO datasets}
\usage{
ato_manifest(format = c("df", "yaml", "json"))
}
\arguments{
\item{format}{One of \code{"df"} (default, tidy data frame),
\code{"yaml"}, or \code{"json"}.}
}
\value{
A data frame, YAML string, or JSON string depending on
\code{format}.
}
\description{
Every call to a data function (\code{ato_individuals()},
\code{ato_companies()}, etc.) appends one row to the session
manifest, recording URL, dataset title, CKAN resource and
package IDs where resolvable, SHA-256 of the cached file,
size, retrieval timestamp, and the snapshot pin set via
\code{\link[=ato_snapshot]{ato_snapshot()}}. Duplicate URLs within a session are
deduplicated (last fetch wins).
}
\details{
Attach the output to your paper's appendix, deposit it to
Zenodo with \code{\link[=ato_deposit_zenodo]{ato_deposit_zenodo()}} to mint a DOI, or export
with \code{\link[=ato_manifest_write]{ato_manifest_write()}} for CI artefacts.
}
\examples{
\donttest{
op <- options(ato.cache_dir = tempdir())
ato_manifest_clear()
ato_snapshot("2026-04-24")
try(ato_individuals(year = "2022-23"))
ato_manifest()
options(op)
}
}
\seealso{
Other reproducibility: 
\code{\link{ato_deposit_zenodo}()},
\code{\link{ato_manifest_clear}()},
\code{\link{ato_manifest_write}()},
\code{\link{ato_sha256}()},
\code{\link{ato_snapshot}()}
}
\concept{reproducibility}
